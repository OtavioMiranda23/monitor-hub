# Configurações do ambiente
PORT=8080
SPRING_PROFILES_ACTIVE=prod
DOCKER_IMAGE=ghcr.io/seu-usuario/monitor-hub-springboot:latest

# Segredo usado para assinar os tokens JWT (HS256). Troque por um valor forte e aleatório.
JWT_SECRET=
